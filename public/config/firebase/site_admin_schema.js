export const SITE_ADMINS_COLLECTION = "siteAdmins";

export const siteAdminSchema = Object.freeze({
  documentId: "normalized_email",
  fields: Object.freeze({})
});

export default {
  SITE_ADMINS_COLLECTION,
  siteAdminSchema
};
